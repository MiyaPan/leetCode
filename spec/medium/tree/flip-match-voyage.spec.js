
import {flipMatchVoyage, flipMatchVoyage1} from '../../../src/medium/flip-match-voyage';
import {createTreeByLevelTraseval} from '../../../src/utils/binaryTree';

describe('flipMatchVoyage', () => {
    // const tree = createTreeByLevelTraseval([1,2,3]);

    // test("flipMatchVoyage return", () => {
    //     expect(flipMatchVoyage(tree, [1,3,2])).toEqual([1]);
    // });
    // const tree2 = createTreeByLevelTraseval([1,2,null,3]);

    // test("flipMatchVoyage return", () => {
    //     expect(flipMatchVoyage(tree2, [1,2,3])).toEqual([]);
    // });
    // const tree3 = createTreeByLevelTraseval([1,2,3]);

    // test("flipMatchVoyage return", () => {
    //     expect(flipMatchVoyage(tree3, [1,2,3])).toEqual([]);
    // });
    // const tree4 = createTreeByLevelTraseval([1,2,null,3]);

    // test("flipMatchVoyage return", () => {
    //     expect(flipMatchVoyage(tree4, [1,3,2])).toEqual([-1]);
});

// describe('flipMatchVoyage1', () => {
//     const tree = createTreeByLevelTraseval([1,2,3]);

//     test("flipMatchVoyage return", () => {
//         expect(flipMatchVoyage1(tree, [1,3,2])).toEqual([1]);
//     });
// });